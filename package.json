{
  "name": "instafetcher",
  "version": "1.0.0",
  "description": "The project appears to be a Node.js application that interacts with the Instagram Graph API to fetch media details for a user's Instagram account.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ritik Ranjan",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  }
}
